import { TipovehiculoPipe } from './tipovehiculo.pipe';

describe('TipovehiculoPipe', () => {
  it('create an instance', () => {
    const pipe = new TipovehiculoPipe();
    expect(pipe).toBeTruthy();
  });
});
